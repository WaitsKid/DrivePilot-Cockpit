# YuNet model directory

Place one of the following files here:

- `face_detection_yunet_2026may.onnx` (preferred when supported)
- `face_detection_yunet_2023mar.onnx` (fixed-input compatibility fallback)

The model file is intentionally not included in the public project archive.
